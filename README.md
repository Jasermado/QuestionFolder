# QuestionFolder
a visible, configurable Windows study assistant that lets you select a region of the screen, send the captured question to an OpenAI vision-capable model, and receive an explanation in the console and clipboard not used for cheating at all
